# Category: Conditional Branching

This section compiles architectural patterns that manipulate the regex engine's execution path to simulate programming constructs like `switch/case` and multiplexing.

## Available Patterns

*   **[No-Pipe Multiplexer](./no_pipe_multiplexer)**: Dynamic sequential routing without performance overhead.
